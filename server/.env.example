# Server Configuration
PORT=3000
MONGODB_URI=mongodb://localhost:27017/your_database_name

# JWT Configuration
JWT_SECRET=your_jwt_secret

# Add other environment variables your server needs (without actual values)
